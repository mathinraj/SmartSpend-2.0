import Preferences from '../../../views/Preferences';

export const metadata = {
  title: 'Preferences',
  description: 'Customize SpendTrak to fit your needs. Set currency, themes, reminders, app lock, and manage your data exports.',
};

export default function PreferencesPage() {
  return <Preferences />;
}
