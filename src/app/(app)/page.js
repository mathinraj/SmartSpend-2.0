import Home from '../../views/Home';

export const metadata = {
  title: 'SpendTrak — Free Expense Tracker | Budget & Finance Manager',
  description: 'Free expense tracker that works offline. Track spending, manage accounts, split bills, plan payments, and analyze your finances — all private, no sign-up needed.',
};

export default function HomePage() {
  return <Home />;
}
