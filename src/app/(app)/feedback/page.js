import Feedback from '../../../views/Feedback';

export const metadata = {
  title: 'Feedback',
  description: 'Report bugs, request features, or share your thoughts about SpendTrak. Your feedback helps make the app better.',
};

export default function FeedbackPage() {
  return <Feedback />;
}
