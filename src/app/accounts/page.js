import Accounts from '../../views/Accounts';

export const metadata = {
  title: 'Accounts',
  description: 'Manage your bank accounts, wallets, and cards. Track balances across all your financial accounts in one dashboard.',
};

export default function AccountsPage() {
  return <Accounts />;
}
