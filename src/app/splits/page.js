import SplitTracker from '../../views/SplitTracker';

export const metadata = {
  title: 'Split Tracker',
  description: 'Split expenses with friends and roommates. Track who owes what, record settlements, and keep shared finances clear.',
};

export default function SplitsPage() {
  return <SplitTracker />;
}
